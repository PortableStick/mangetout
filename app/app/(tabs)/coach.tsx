import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function CoachScreen() {
  return (
    <PlaceholderScreen
      title="Coach"
      icon="sparkles-outline"
      subtitle="Le coach IA (propose → confirme → applique) arrive au Milestone 10."
    />
  );
}
